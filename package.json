{
  "name": "media-server",
  "version": "1.0.0",
  "description": "",
  "main": "built/server.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "lint": "tslint -p ./tsconfig.json src/**/*.ts",
    "start": "node .",
    "debug": "node --nolazy --inspect-brk=9229 ."
  },
  "devDependencies": {
    "tslint": "5.11.0",
    "typescript": "3.0.3"
  },
  "dependencies": {
    "@types/node": "^10.3.1",
    "rtsp-streaming-server": "^2.0.3"
  }
}
